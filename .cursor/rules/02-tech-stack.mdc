---
description: Tech stack for this project — chosen based on a "best-for-vibecoding" analysis
alwaysApply: true
---

# Tech Stack

> This file is **filled per project**. The template is stack-agnostic.
> The concrete stack is chosen after a "best-for-vibecoding" analysis (speed of iteration, ecosystem maturity, AI-tooling compatibility, deployment friction, cost).

## Decision rule

Before picking a stack, run the analysis and record the result in `memory/03-architecture.md` with justification. Default palette (only a reference, not mandatory):

- Framework: Next.js + TypeScript
- Database: Supabase / Postgres
- Payments: Stripe
- Hosting / edge: Vercel, Railway, Cloudflare

## This project

- **Platform:** _TBD_  (one of: `web`, `mobile`, `cross`)
- **Framework:** _TBD_  (web: Next.js / Remix / Astro / Vite React; mobile: Expo SDK + RN; cross: both)
- **Language:** _TBD_  (TypeScript strict recommended for both web and mobile)
- **Database:** _TBD_
- **Auth:** _TBD_
- **Payments:** _TBD_
- **Hosting:** _TBD_  (web: Vercel / Railway / Cloudflare; mobile: EAS Build + App Store + Play Store)
- **Testing:** _TBD_  (web: Vitest / Playwright; mobile: Jest + React Native Testing Library + Maestro for e2e)
- **Observability:** _TBD_
- **Analytics:** _TBD_

### Design + UI (platform-aware)

- **Design system (web):** shadcn/ui (via official MCP + Skill). Only applies if Platform is `web` or `cross`.
- **Design system (mobile):** [react-native-reusables](https://reactnativereusables.com) (RNR) + NativeWind v4. Only applies if Platform is `mobile` or `cross`. Same `npx shadcn init` CLI — Expo auto-detected.
- **Prototyping tool:** Claude Design (`claude.ai/design`) — works for both tracks; `prototype-designer` tunes the prompt based on Platform. Invoke via `/mm-design`.
- **DESIGN.md:** portable cross-tool export generated from `memory/14` via `scripts/export-design-md`.

### Mobile-specific (only fill if Platform = `mobile` or `cross`)

- **Expo SDK:** _TBD_  (default target: latest stable, currently 52+)
- **Navigation:** Expo Router (file-based, type-safe)  — default per `08-design-system.mdc`
- **Styling:** NativeWind v4  — default per `08-design-system.mdc`
- **Animations:** React Native Reanimated 3
- **Gestures:** React Native Gesture Handler
- **Safe-area:** `react-native-safe-area-context`
- **State mgmt:** _TBD_  (Zustand / Jotai / Redux Toolkit)
- **Data fetching:** _TBD_  (TanStack Query recommended)
- **Secure storage:** `expo-secure-store` (tokens, PII)
- **Notifications:** _TBD_  (Expo Notifications / OneSignal / Firebase)
- **Native modules required:** _TBD_  (if any, you need dev client; otherwise Expo Go works)
- **Build / submit:** EAS Build + EAS Submit

## Constraints

_TBD — any libraries or services that are forbidden for this project (e.g. GPL licensing, data-residency requirements)._

---

## Universal Conventions (apply ONLY when stack is JS / TypeScript)

> Ignore this block if the project uses a different language. Delete or adapt once the stack is confirmed.

### Language
- TypeScript in **strict mode** (`"strict": true` + `"noUncheckedIndexedAccess": true`).
- `const` and `let` only, never `var`.
- Prefer type inference; annotate only public APIs and exported types.
- No `any`. Use `unknown` + narrowing when the type is truly unknown.

### Components & modules (React / Next.js)
- Functional components + hooks only. No class components.
- **Named exports only.** No default exports (avoids rename drift and improves refactoring).
- Exception: Next.js page/layout/route files that require default exports by convention.

### File & folder naming
- Folders: `kebab-case`
- React components: `PascalCase.tsx`
- Hooks: `camelCase` starting with `use` (e.g. `useAuth.ts`)
- Utilities, types, constants: `kebab-case.ts` (e.g. `date-utils.ts`)

### Imports
Order (separate with a blank line):
1. React / Next / framework primitives
2. Third-party packages
3. Internal absolute imports (`@/…`)
4. Relative imports (`./`, `../`)

Alphabetical within each group. Prefer absolute paths with an `@/` alias over long relative chains. Avoid barrel files (`index.ts`) for leaf modules — they hurt tree-shaking and introduce circular imports.

### Style
- Early returns over nested `if`.
- Guard clauses first, happy path last.
- Destructure props at the top of the function.
- No magic numbers — extract to a named constant.

## Notes

_TBD_
