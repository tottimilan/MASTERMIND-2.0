---
description: Claude Opus + MCP integration rules — when and how to use external tools and the Claude bridge
alwaysApply: true
---

# Claude + MCP Integration

> Defines when to delegate to Claude Opus and which MCP servers to use.

## When to delegate to Claude Opus

Use Claude Opus (Claude Desktop, Claude Projects, or an MCP bridge) for:

- Deep multi-angle analysis ("desgarre").
- Strategic decisions and risk assessments.
- Long-form documents (PRDs, architecture overviews, scenario planning).
- "Brutal honesty" reviews and pivot exploration.
- Anything that benefits from extended thinking.

Use Cursor's local model for:

- Daily coding, refactoring, bug fixing.
- Surgical edits with clear success criteria.
- Running and interpreting tests.

## MCP servers used by this project

_TBD — list the MCPs configured in `claude-side/mcp-config.json` and `.cursor/mcp.json`._

Typical defaults:

- **Context7** — automatic, for any external library or API. Never guess API signatures.
- **Playwright** — only for real browser verification of critical flows. Always specify URL, viewport, and selectors.
- **Memory Graph** — cross-project lessons. Never dump sensitive data there.
- **GitHub** — issues, PRs, code context.
- **Notion / Linear** — optional, depending on the project's operations stack.

### Activation-on-demand — `task-master-ai`

- **`task-master-ai` (eyaltoledano/claude-task-master)** — AI-driven task management MCP. Parses a PRD (e.g. `memory/00-project-brief.md` + `docs/product/`) into dependency-aware tasks, then drives the execution loop (`next_task`, `get_task`, `set_task_status`, `expand_task`, `update_subtask`, `parse_prd`).

**When to activate it in a project** (not one-time; per-project):
1. The project is leaving Definition and entering **MVP execution**.
2. The approved `implementation-planner` plan has **≥ 10 tasks** (or the plan explicitly declares fan-out to many slices).
3. `subagent-dispatcher` is about to take over plan execution and benefits from dependency-aware task state management.

Before those three conditions: not worth the extra ~5–21k tokens and extra moving part. Plan Mode + `subagent-dispatcher` walking the plan file directly is enough.

**How to install it for a project** (installer script handles everything):

```powershell
pwsh -File scripts/install-taskmaster.ps1                   # mode=core (7 tools, ~5k tokens)
pwsh -File scripts/install-taskmaster.ps1 -Mode standard    # 15 tools, ~10k tokens
pwsh -File scripts/install-taskmaster.ps1 -ClaudeCodeAuth   # no API key, uses Claude Code OAuth
```

```bash
bash scripts/install-taskmaster.sh
bash scripts/install-taskmaster.sh --mode standard
bash scripts/install-taskmaster.sh --claude-code-auth
```

The installer:
- Adds the `task-master-ai` entry to `.cursor/mcp.json` (idempotent — leaves existing entries alone).
- Creates `.taskmaster/docs/prd.md` with a template PRD if none exists.
- Appends `.taskmaster/runtime/` to `.gitignore`.
- Prints exact next-step commands.

**Integration contract with other skills:**
- `implementation-planner` emits a PRD-shaped document compatible with `parse-prd`.
- `subagent-dispatcher` uses `next_task` / `set_task_status` when task-master is active; otherwise walks the plan file.
- `feature-breakdown` slices can be parsed with `--append` to add tasks to an existing tasks.json.

**Tool loading modes:**
| Mode | Tools | Context cost | When |
|---|---|---|---|
| `core` | 7 | ~5k tokens | **Default**. Day-to-day dispatch loop. |
| `standard` | 15 | ~10k tokens | Complex projects that benefit from expansion/analysis tools. |
| `all` | 36 | ~21k tokens | Rare. Only when research and tags are used heavily. |

**Scope of `.taskmaster/` folder:** lives alongside `memory/` and `docs/`, not inside them. PRD and `tasks.json` are committed. `runtime/` and caches are gitignored.

## Rules

- Always use Context7 when touching library code.
- Never use Playwright MCP for scraping or bulk browsing. Only for verification.
- When Claude Opus produces a deliverable, save it to `memory/` or `docs/` and run the `memory-updater` skill.
- Secrets for MCP servers live in environment variables, never in committed files.

---

## Cross-project Memory Protocol

The project's `memory/` folder is **per-project intelligence**. It should not accumulate patterns that generalize across projects. Those belong in a **shared cross-project memory** that lives **outside the template** and is consulted by multiple projects.

### Location

Default convention: `~/.mastermind/global/` (user-home, outside any project repo).

Recommended structure:

```
~/.mastermind/global/
├── lessons.md         # What worked or failed, with evidence and context
├── patterns.md        # Reusable architectural / product / workflow patterns
├── pitfalls.md        # Anti-patterns observed repeatedly across projects
├── stacks.md          # Stack choices taken across projects, with outcomes
└── vendors.md         # Third-party providers used, with verdicts
```

Files are **plain Markdown** and are the source of truth. When the **Memory Graph MCP** (Zep / Cognee / Basic Memory) is installed, these files may be mirrored into it; the Markdown remains authoritative.

### Promotion criteria

An entry is promoted from a project's `memory/` to `~/.mastermind/global/` only when **all three** hold:

1. **Project-agnostic.** Strip project-specific nouns; the lesson still reads as useful.
2. **Evidence-backed.** At least one concrete occurrence, with a link to a post-mortem, decision, ADR, or retrospective.
3. **Actionable.** It changes a future decision. If it only informs, it stays in the project.

Most project findings do **not** qualify. They belong in `memory/07-decisions-log.md` and stay there.

### Consumption (when to read the global memory)

- **At the start of a new project** — `project-deep-audit` reads `lessons.md` and `pitfalls.md` and surfaces relevant items under a "Cross-project signals" section.
- **Before external research** — `research-first` consults `vendors.md` and `stacks.md` before running a web search. If the vendor or stack was already decided against, flag it.
- **Before topology proposals** — `architecture-mapper` consults `patterns.md` when proposing a topology or service split.
- **Before writing a new skill** — `skill-creator` consults `patterns.md` to check whether the skill already exists in another project.

### Privacy and safety

`~/.mastermind/global/` must never contain:

- Secrets, tokens, API keys, credentials.
- Client names or confidential product details.
- PII, PHI, or payment data.

Use neutral references: *"a marketplace SaaS in the logistics domain"*, not *"Project Acme for Client X"*. When exporting a lesson, replace the specific nouns before the commit.

### Change control

- `~/.mastermind/global/` is versioned in its own git repo (private).
- Promotions are single commits: `lesson: <topic>` or `pattern: <topic>`.
- Supersede, do not delete: when a lesson becomes obsolete, mark it superseded in place and append the new entry. Historical context stays searchable.
